# 互斥原子操作
## 整形原子操作
```c
1. 设置原子变量的值
void atomic_set(atomic_t *v,int i);//设置原子变量操作为i
atomic_t v=ATOMIC_INIT(0); //定义原子变量V并初始化为0
2. 获取原子变量的值
atomic_read(atomic_t *v);
3. 原子变量加、减
void atomic_add(int i,atomic_t *v); //原子变量增加i
void atomic_sub(int i,atomic_t *v); //原子变量减少i
4. 原子变量自增/自减
void atomic_inc(atomic_t *v); //原子变量增加1
void atomic_dec(atomic_t *v); //原子变量减少1
5. 操作并测试
int atomic_inc_and_test(atomic_t *v);
int atomic_dec_and_test(atomic_t *v);
int atomic_sub_and_test(atomic_t *v);//减操作
//加减之后，返回值是否为0，为0则ture,不为0返回false

5. 操作并测试
int atomic_add_return(int i,atomic_t *v);//加操作
int atomic_sub_return(int i,atomic_t *v);//减操作
int atomic_inc_return(atomic_t *v);
int atomic_dec_return(atomic_t *v);
//加减之后，返回新的值
```
```c
使用原子变量使设备只能被一个进程打开
static atomic_t var = ATOMIC_INIT(1);//定义原子变量
static int open()
{
    if(!atomic_dec_and_test(&var)) //已经被打开后atomic_dec_and_test(&var)小于0为false,取反为ture
    {
        atomic_inc(&var);
        return -EBUSY; //失败
    }
    return 0;//成功
}
static int release()
{
    atomic_inc(&var); //+1释放设备，使其能重新打开
    return 0；
}
```
## 2自旋锁
```c
1. 定义自旋锁
spinlock_t lock;
2. 初始化自旋锁
spin_lock_init(lock);//动态初始化自旋锁
3. 获取自旋锁
spin_lock(lock)
如果能获取lock就立马返回，否则一直等待
spin_trylock(lock)
尝试获取自旋锁，如果能立即获得锁。返回ture,失败false,不在一直等待
4. 释放自旋锁
spin_unlock(lock)

自旋锁使用
定义
初始化
获取锁
......
释放锁
```

补充知识
1. 关中断
可以通过下面两个函数中的其中任何一个 关闭当前处理器上的所有中断处理， 这两个函数定义在 <asm/system.h>中:
```c
void local_irq_save(unsigned long flags);
void local_irq_disable(void);
```

 
local_irq_save的调用把当前的中断状态（开或关）保存到flags中，然后禁用当前处理器上的中断。注意, flags 被直接传递, 而不是通过指针来传递，这是由于 local_irq_save被实现为宏 。
local_irq_disable不保存状态而关闭本地处理器上的中断发送; 只有我们知道中断并未在其他地方被禁用的情况下，才能使用这个版本。
 
 
 
2. 开中断
可通过如下函数打开中断:
```c
void local_irq_restore(unsigned long flags);
void local_irq_enable(void);
 
local_irq_restore将 保存的flags状态值恢复（即 local_irq_save的入参flag ）, 恢复之前的状态（开或关）。
local_irq_enable则无条件打开中断。
```
在一个关闭中断的环境中调用 local_irq_disable和 local_irq_enable后会破坏之前的中断响应状态。尽管调用 local_irq_disable前是关中断的环境，但是在调用 local_irq_enable后却变成开中断，这显然不是我们希望的 。
**调用 local_irq_restore后不一定会开启中断，只会恢复调用 local_irq_save之前的中断状态，如果调用 local_irq_save之前是开中断，那么就打开中断； 如果调用 local_irq_save之前是关中断，那么就关闭中断。
所以 local_irq_save和 local_irq_restore会更安全**。
没有方法全局禁用整个系统的所有中断。 内核开发者认为关闭所有中断的代价太高，因此没有必要提供这个能力。

```c
spin_lock_irq() = spin_lock()+local_irq_disable()  
spin_unlock_irq() = spin_unlock()+local_irq_enable()

spin_lock_irqsave() = spin_lock()+local_irq_save()  
spin_unlock_irqrestore() = spin_unlock()+local_irq_restore()

spin_lock_bh() = spin_lock()+local_bh_disable()  
spin_unlock_bh() = spin_unlock()+local_bh_enable()
```
应用场景
进程和中断可能同时访问一篇临界资源，我们需要在进程上下文中调用spin_lock_irqsave();/spin_unlock_irqrestore(),中断中使用spin_lock_irq()/spin_unlock_irq()
**实际最好使用spin_lock_irqsave();/spin_unlock_irqrestore()**
自旋锁需要注意问题
1. 自旋锁的本质时死循环等待，临界区过大会导致系统死等时间加长，降低系统性能
2. 自旋锁容易造成死等
3. 自旋锁临界区不能调用进程调度函数，如果进程获取自旋锁后阻塞（调用copy_from_usr,copy_to_usr,kmalloc,msleep）可能造成内核崩溃
   
## 3读写自旋锁

有一个问题，既然自旋锁临界区不能使用（调用copy_from_usr,copy_to_usr），这些函数又是读写常调用函数，为什么不怕内核崩溃

## 4RCU读-复制-更新
写的时候先复制一个副本，更改副本，最后使用回调机制在适当的时机吧指向原来数据的指针，指向更改数据后的指针。这个时机就是所有CPU都退出共享数据操作，等待适当时机这一时期称为宽限期
>https://zhuanlan.zhihu.com/p/89439043

RCU机制暂时了解一下，后续继续更新2023、11、16

## 5信号量操作
```c
1. 定义信号量
struct semaphore sem;
2. 初始化信号量
void sema_init(struct semaphore *sem,int val);
3. 获取信号量
void down(struct semaphore *sem);
用于获取信号量 他会导致进程休眠，且不能被信号打断，**不能在中断上下文中使用**

int down_interruptible(struct semaphore *sem);
用于获取信号量 他会导致进程休眠，能被信号打断，来信号后函数返回值为非0

int down_trylock(struct semaphore *sem);
尝试获得信号量，成功返回0，不成功返回非0，不会导致调用者睡眠，可以在中断上下文中使用
在使用down_interruptible();如果返回非0，通常立即返回-ERESTARTSYS;
if(down_interruptible(&sem))
return -ERESTARTSYS;

void up(struct semaphore *sem);
释放信号，唤醒等待者

```


## 6互斥体
```c
1. 定义初始化结构体
struct mutex my_struct;
mutex_init(&my_mutex);
2. 获取互斥体
void mutex_lock(struct mutex *lock);
void mutex_lock_interrputible(struct mutex *lock);
void mutex_trylock(struct mutex *lock);
3. 释放互斥体
void mutex_unlock(struct mutex *lock)
```


互斥体和自旋锁
1. 临界区大用互斥锁，临界区小用自旋锁
2. 互斥锁临界区中可以阻塞任务调度，自旋锁临界区中不能有阻塞，或者任务调度
3. **被保护共享资源在中断或者软中断情况下使用，只能使用自旋锁**








